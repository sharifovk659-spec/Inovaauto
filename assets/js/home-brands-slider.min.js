(function () {  'use strict';   var PX_MOBILE =0.55;  var PX_DESKTOP =0.45;  var USER_PAUSE_MS =3200;  var DRAG_THRESHOLD =4;   function initBrandsSlider(section) {    var viewport =section.querySelector('.ia-brands-slider');    var inner =section.querySelector('.ia-brands-slider__inner');    if (!viewport||!inner) {      return;   }      var reducedMotion =window.matchMedia('(prefers-reduced-motion: reduce)').matches;    var mobileMq =window.matchMedia('(max-width: 991.98px)');    var pxPerFrame =mobileMq.matches?PX_MOBILE:PX_DESKTOP;    var offset =0;    var tabPaused =false;    var userActive =false;    var resumeTimer =null;    var rafId =null;    var dragging =false;    var dragStartX =0;    var dragStartOffset =0;    var pointerId =null;    var touchPending =false;     function firstTrack() {      return inner.querySelector('.ia-brands-slider__track');   }      function trackWidth() {      var track =firstTrack();      if (!track) {        return 0;     }       return track.getBoundingClientRect().width;   }      function loopWidth() {      return trackWidth();   }      function ensureTracksFill() {      var track =firstTrack();      if (!track) {        return;     }       var unit =trackWidth();      var viewW =viewport.clientWidth;      if (unit<=0||viewW<=0) {        return;     }       var tracks =inner.querySelectorAll('.ia-brands-slider__track');      var needed =Math.max(2,Math.ceil((viewW*2)/unit)+1);      while (tracks.length<needed) {        var clone =track.cloneNode(true);        clone.setAttribute('aria-hidden','true');        inner.appendChild(clone);        tracks =inner.querySelectorAll('.ia-brands-slider__track');     }    }      function applyTransform() {      inner.style.transform ='translate3d('+(-offset)+'px,0,0)';   }      function normalizeOffset() {      var half =loopWidth();      if (half<=0) {        return;     }       while (offset>=half) {        offset-=half;     }       while (offset<0) {        offset+=half;     }    }      function tick() {      rafId =window.requestAnimationFrame(tick);      if (tabPaused||userActive||reducedMotion||dragging) {        return;     }       offset+=pxPerFrame;      normalizeOffset();      applyTransform();   }      function pauseForUser(ms) {      userActive =true;      if (resumeTimer) {        window.clearTimeout(resumeTimer);     }       resumeTimer =window.setTimeout(function () {        userActive =false;     },ms);   }      function updateSpeed() {      pxPerFrame =mobileMq.matches?PX_MOBILE:PX_DESKTOP;   }      function endDrag() {      if (!dragging&&!touchPending) {        return;     }       dragging =false;      touchPending =false;      pointerId =null;      viewport.classList.remove('is-dragging');      pauseForUser(USER_PAUSE_MS);   }      viewport.addEventListener('touchstart',function (e) {      if (!e.touches.length) {        return;     }       touchPending =true;      dragStartX =e.touches[0].clientX;      dragStartOffset =offset;   },{passive:true});     viewport.addEventListener('touchmove',function (e) {      if (!e.touches.length||(!touchPending&&!dragging)) {        return;     }       var dx =dragStartX-e.touches[0].clientX;      if (touchPending&&Math.abs(dx)>=DRAG_THRESHOLD) {        dragging =true;        userActive =true;        touchPending =false;        viewport.classList.add('is-dragging');     }       if (!dragging) {        return;     }       offset =dragStartOffset+dx;      normalizeOffset();      applyTransform();   },{passive:true});     viewport.addEventListener('touchend',endDrag,{passive:true});    viewport.addEventListener('touchcancel',endDrag,{passive:true});     viewport.addEventListener('pointerdown',function (e) {      if (e.pointerType ==='touch') {        return;     }       if (pointerId!==null) {        return;     }       pointerId =e.pointerId;      dragStartX =e.clientX;      dragStartOffset =offset;      if (viewport.setPointerCapture) {        viewport.setPointerCapture(e.pointerId);     }    });     viewport.addEventListener('pointermove',function (e) {      if (e.pointerId!==pointerId) {        return;     }       var dx =dragStartX-e.clientX;      if (!dragging&&Math.abs(dx)>=DRAG_THRESHOLD) {        dragging =true;        userActive =true;        viewport.classList.add('is-dragging');     }       if (!dragging) {        return;     }       offset =dragStartOffset+dx;      normalizeOffset();      applyTransform();   });     viewport.addEventListener('pointerup',function (e) {      if (e.pointerId!==pointerId) {        return;     }       endDrag();   });     viewport.addEventListener('pointercancel',function (e) {      if (e.pointerId!==pointerId) {        return;     }       endDrag();   });     document.addEventListener('visibilitychange',function () {      tabPaused =document.hidden;   });     if (typeof mobileMq.addEventListener ==='function') {      mobileMq.addEventListener('change',updateSpeed);   } else if (typeof mobileMq.addListener ==='function') {      mobileMq.addListener(updateSpeed);   }      window.addEventListener('resize',function () {      updateSpeed();      ensureTracksFill();      normalizeOffset();      applyTransform();   });     function startWhenReady(attempts) {      attempts =attempts||0;      ensureTracksFill();      if (loopWidth()<=0&&attempts<60) {        window.setTimeout(function () {          startWhenReady(attempts+1);       },100);        return;     }       offset =0;      inner.classList.add('is-js-active');      applyTransform();      if (!reducedMotion) {        rafId =window.requestAnimationFrame(tick);     }    }      if (document.readyState ==='complete') {      startWhenReady(0);   } else {      window.addEventListener('load',function () {        startWhenReady(0);     });   }  }    function bootAll() {    document.querySelectorAll('.ia-brands-section[data-ia-brands-slider]').forEach(initBrandsSlider); }    if (document.readyState ==='loading') {    document.addEventListener('DOMContentLoaded',bootAll); } else {    bootAll(); }})();
